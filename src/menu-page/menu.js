import { content } from "../DOM/local-weather";


export function showMenuPage() {
    const menuContainer = document.createElement("div");
    menuContainer.style.backgroundColor = "black"
    menuContainer.style.height = "100vh"
    menuContainer.style.width = "100vw"

    // Fix design

    // Add div for 3 dots at the top

    // Add heather header

    // Add search bar

    // Add locations (CREATE ARRAY)


    content.appendChild(menuContainer)
}